# Canonical Graph Fixture

This deliberately tiny, three-document corpus exercises the real matlatl
pipeline against an independently hand-authored graph oracle.

## Topology

`README.md` links to the install guide, the install guide links to the operation
guide, and the operation guide links back here. This page is the root.

Continue with [the install guide](docs/install.md).
