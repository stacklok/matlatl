{
  "schemaVersion": 1,
  "taskId": "webhook-egress",
  "cases": [
    {"name":"correct","expectPass":true,"replacements":[{"path":"relay/webhook.go","old":"if ip != nil && !devAllowHTTP && (ip.IsPrivate() || ip.IsLoopback() || ip.IsLinkLocalUnicast() || ip.IsUnspecified()) {","new":"if ip != nil && (ip.IsPrivate() || ip.IsLoopback() || ip.IsLinkLocalUnicast() || ip.IsUnspecified()) {"}]},
    {"name":"alternative","expectPass":true,"replacements":[{"path":"relay/webhook.go","old":"if ip != nil && !devAllowHTTP && (ip.IsPrivate() || ip.IsLoopback() || ip.IsLinkLocalUnicast() || ip.IsUnspecified()) {","new":"if ip != nil && (ip.IsLoopback() || ip.IsPrivate() || ip.IsUnspecified() || ip.IsLinkLocalUnicast()) {"}]},
    {"name":"plausible-wrong","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"if ip != nil && !devAllowHTTP && (ip.IsPrivate() || ip.IsLoopback() || ip.IsLinkLocalUnicast() || ip.IsUnspecified()) {","new":"if ip != nil && (ip.IsPrivate() || ip.IsLoopback()) {"}]},
    {"name":"mutated","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"func ValidateWebhook(raw string, devAllowHTTP bool) error {","new":"func ValidateWebhook(raw string, devAllowHTTP bool) error { // mutated fixture"}]},
    {"name":"init-exit","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"package relay","new":"package relay\n\nimport \"os\"\n\nfunc init() { os.Exit(0) }"}]},
    {"name":"testmain-bypass","expectPass":false,"replacements":[{"path":"relay/relay_test.go","old":"import \"testing\"","new":"import (\n  \"os\"\n  \"testing\"\n)\n\nfunc TestMain(m *testing.M) { os.Exit(0) }"}]},
    {"name":"hidden-embed","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"package relay","new":"package relay\n\nimport _ \"embed\"\n\n//go:embed hidden_test.go\nvar hiddenBytes []byte"}]},
    {"name":"protocol-cheat","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"package relay","new":"package relay\n\nimport \"os\"\n\nfunc init() { _, _ = os.Stdout.Write([]byte(\"{}\\n\")) }"}]},
    {"name":"timeout","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"package relay","new":"package relay\n\nfunc init() { select {} }"}]},
    {"name":"output-flood","expectPass":false,"replacements":[{"path":"relay/webhook.go","old":"package relay","new":"package relay\n\nfunc init() { for { println(\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\") } }"}]}
  ]
}
