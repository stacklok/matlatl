# Binaries
/bin/
/dist/
# The built binary at the repo root (anchored so it does NOT match the
# cmd/doctopus/ source directory, which must stay tracked).
/doctopus
*.exe

# Test / coverage artifacts
*.out
coverage.*
/tmp/

# Editor / OS
.idea/
.vscode/
.DS_Store

# doctopus's own generated output
/out/
