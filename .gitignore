# Binaries
/bin/
/dist/
# The built binary at the repo root (anchored so it does NOT match the
# cmd/matlatl/ source directory, which must stay tracked).
/matlatl
*.exe

# Test / coverage artifacts
*.out
coverage.*
/tmp/

# Editor / OS
.idea/
.vscode/
.DS_Store

# matlatl's own generated output
/out/
/eval/out/
/eval/agent-outcome/runs/
