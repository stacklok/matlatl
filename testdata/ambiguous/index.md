# Index

This corpus's only finding is an ambiguous wikilink: [[notes]] matches two
documents with the same basename. There are no broken links or anchors, so it
exits 0 by default and 1 only under --strict (ADR 0005).
