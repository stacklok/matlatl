# ADRs

Index of architecture decision records in this folder. Note this index does
NOT link its siblings — so under `--strict` (no-vouch directory links), the
siblings below surface as unreachable; under the default policy they are reached
via the directory link from the parent README.
